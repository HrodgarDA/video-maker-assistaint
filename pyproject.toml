[project]
name = "video-moments-poc"
version = "0.1.0"
description = "POC: batch video moments + thumbnails + inspection confidence (local)"
requires-python = ">=3.11"
dependencies = [
  "streamlit>=1.36",
  "opencv-python>=4.9",
  "numpy>=1.26",
]

[tool.setuptools.packages.find]
where = ["src"]